g = 9.81;

grid on

[X,Y] = meshgrid(0:2:38,-19:2:19);
Tresh = 0.1;
U = Y;
V = zeros(length(U),length(U)) -g;

acceptIndex = X >=

q = quiver(Y,X,V,U,LineWidth=0.01);
%q.ShowArrowHead = 'off';
%q.Marker = '^';

ylim([-4 13]); xlim([-20 20]);